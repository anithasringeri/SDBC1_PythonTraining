# Example 8: Iterating through a set
# Objective: Print each element using for-else.
# Outcome: Prints all elements and “All elements printed.”
# Iterate through set elements

items= {"book", "pen", "pencil", "box"}
for item in items:
    print(item)
else:
    print("All items printed")